import React from 'react';

import ValidationInput from '../components/validation/ValidationInput';

const Validation = ({ history }) => {
  return (
    <>
      <ValidationInput history={history} />
    </>
  );
};

export default Validation;
